# Brain (LLM) — Google Gemini SDK (primary provider) + embeddings
google-genai>=2.17.0

# Fallback providers (Groq, OpenRouter) — OpenAI-compatible API
openai>=3.3.1

# Interface — Telegram bot (async)
python-telegram-bot>=21.0

# Voice (text -> speech), free
edge-tts>=7.2.8

# Load variables from .env
python-dotenv>=1.2.3

# Use the OS trust store (fixes TLS behind a corporate proxy)
truststore>=0.10.4

# Timezone database (so ZoneInfo works on minimal systems, e.g. the VM)
tzdata>=2024.1

# Web search tool (DuckDuckGo, no API key)
ddgs>=9.15.0

# Google Calendar & Gmail tools (optional — used only if GOOGLE_OAUTH_CLIENT set)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.4.0

# PDF text extraction for the knowledge base
pypdf>=6.16.2

# Document generation (E.V. creates PDF / Word files) — pure-Python, no system libs
reportlab>=4.0.0
python-docx>=1.1.0

# Web interface (use E.V. from any browser)
fastapi>=0.110.0
uvicorn>=0.52.4
python-multipart>=0.0.9

# PWA app icons (rendered as PNG) for the installable web app
pillow>=12.3.0

# Web Push notifications (reminders with the app closed)
pywebpush>=2.4.0

# HTTP client for local_agent.py (the PC-side task executor)
requests>=2.31.0

# Real browser automation for local_agent.py 'browser' tasks (optional — only
# needed on the machine running local_agent.py, not on the server). After
# installing, also run: playwright install chromium
playwright>=1.62.0

# Tests (dev)
pytest>=8.0.0
